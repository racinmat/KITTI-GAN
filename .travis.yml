language: python
python:
  - "3.4"
  - "3.5"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests

before_script:
  export PYTHONPATH=$PYTHONPATH:$(pwd)
  export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/local/cuda/lib64/

script:
  - python tests/fast_tests.py
